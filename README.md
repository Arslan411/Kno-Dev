# app

## Getting started

First install the dependencies:

```sh
npm install
```

## Running the app

For iOS:

```sh
npm run ios
```

For Android:

```sh
npm run android
```

You can also run the app on a device by using the Expo app.

```sh
npm start
```
